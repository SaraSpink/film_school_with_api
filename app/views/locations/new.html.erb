<h1>Create a New Location Brah!</h1>
<%= form_for @location do |f| %>
<div class="field">
  <%= f.label :name %><br />
  <%= f.text_field :name %>
</div>
<div class="actions">
  <%= f.submit "Submit Location" %>
</div>
<% end %>
